# - Version file for the Valadoc config module used by FIND_PACKAGE().
#

cmake_minimum_required(VERSION @VALADOC_MINIMUM_CMAKE_VERSION@ FATAL_ERROR)

set(PACKAGE_VERSION @VALADOC_VERSION@)

set(PACKAGE_VERSION_COMPATIBLE TRUE)
set(PACKAGE_VERSION_EXACT FALSE)
set(PACKAGE_VERSION_UNSUITABLE FALSE)
if("${PACKAGE_FIND_VERSION}" VERSION_GREATER "${PACKAGE_VERSION}")
  set(PACKAGE_VERSION_COMPATIBLE FALSE)
elseif("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "${PACKAGE_VERSION}")
  set(PACKAGE_VERSION_EXACT TRUE)
endif()
if(NOT PACKAGE_FIND_NAME STREQUAL Valadoc)
  set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
